pub fn rectangle(length: f32, width: f32) -> f32 {
    length * width
}

/// Calculate the area of a triangle given its base and height.
pub fn triangle(base: f32, height: f32) -> f32 {
    base * height / 2.0
}
